import projects from '../../../data/projects';
import { FaExternalLinkAlt, FaGithub } from 'react-icons/fa';

const Projects = () => (
  <section
    id="projects"
    style={{
      minHeight: '100vh',
      background: 'var(--bg-2)',
      padding: 'clamp(64px,10vw,120px) clamp(24px,8vw,120px)',
    }}
  >
    <div style={{ maxWidth: 1100, margin: '0 auto' }}>
      {/* Label */}
      <p className="section-label" style={{ marginBottom: 12 }}>0.4 — Projects</p>

      <h2
        className="font-display"
        style={{
          fontSize: 'clamp(2rem,5vw,3.2rem)', fontWeight: 800,
          color: 'var(--text-bright)', marginBottom: 16, letterSpacing: '-0.02em',
        }}
      >
        Things I've Built
      </h2>
      <p style={{ color: 'var(--text-muted)', fontSize: '0.95rem', marginBottom: 52, maxWidth: 480 }}>
        A selection of projects I'm proud of — from full-stack platforms to data-driven tools.
      </p>

      <div style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fill, minmax(300px, 1fr))',
        gap: 28,
      }}>
        {projects.map(project => (
          <article key={project.id} className="glass-card" style={{ overflow: 'hidden', display: 'flex', flexDirection: 'column' }}>
            {/* Image */}
            {project.image && (
              <div style={{ height: 200, overflow: 'hidden', position: 'relative' }}>
                <img
                  src={project.image}
                  alt={project.title}
                  style={{ width: '100%', height: '100%', objectFit: 'cover', transition: 'transform 0.5s' }}
                  onMouseEnter={e => (e.currentTarget.style.transform = 'scale(1.05)')}
                  onMouseLeave={e => (e.currentTarget.style.transform = 'scale(1)')}
                />
                <div style={{
                  position: 'absolute', inset: 0,
                  background: 'linear-gradient(to top, rgba(8,12,16,0.7), transparent)',
                }} />
              </div>
            )}

            {/* Content */}
            <div style={{ padding: 24, flex: 1, display: 'flex', flexDirection: 'column', gap: 12 }}>
              <h3
                className="font-display"
                style={{ fontSize: '1.2rem', fontWeight: 700, color: 'var(--text-bright)' }}
              >
                {project.title}
              </h3>
              <p style={{ color: 'var(--text)', fontSize: '0.88rem', lineHeight: 1.7, flex: 1 }}>
                {project.description}
              </p>

              {/* Tags */}
              <div style={{ display: 'flex', flexWrap: 'wrap', gap: 6, marginTop: 4 }}>
                {project.tags.map(t => <span key={t} className="tag">{t}</span>)}
              </div>

              {/* Links */}
              <div style={{ display: 'flex', gap: 16, marginTop: 8 }}>
                {project.githubUrl && (
                  <a href={project.githubUrl} target="_blank" rel="noopener noreferrer"
                    style={{ color: 'var(--text-muted)', fontSize: '1rem', transition: 'color 0.2s' }}
                    className="hover:[color:var(--accent)]" aria-label="GitHub">
                    <FaGithub />
                  </a>
                )}
                {project.liveUrl && (
                  <a href={project.liveUrl} target="_blank" rel="noopener noreferrer"
                    style={{ color: 'var(--text-muted)', fontSize: '0.9rem', transition: 'color 0.2s' }}
                    className="hover:[color:var(--accent)]" aria-label="Live demo">
                    <FaExternalLinkAlt />
                  </a>
                )}
              </div>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>
);

export default Projects;
