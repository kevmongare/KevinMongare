:root {
  /* Dark mode variables (default) */
  --bg-color: #0a192f;
  --bg-color-light: #112240;
  --text-color: #8892b0;
  --text-color-light: #ffffff;
  --accent-color: #64ffda;
}

/* Light mode overrides */
html.light {
  --bg-color: #f5f5f5;
  --bg-color-light: #ffffff;
  --text-color: #334155;
  --text-color-light: #0a192f;
  --accent-color: #00b894;
}

body {
  background-color: var(--bg-color);
  color: var(--text-color);
}

/* Background utility classes */
.bg-navy { background-color: var(--bg-color); }
.bg-navy-light { background-color: var(--bg-color-light); }
.bg-green-bright { background-color: var(--accent-color); }

/* Text colors */
.text-slate { color: var(--text-color); }
.text-green-bright { color: var(--accent-color); }

/* Hover states */
.hover-text-green-bright:hover { color: var(--accent-color); }
.hover-bg-green-bright:hover { background-color: var(--accent-color); }

/* Borders */
.border-green-bright { border-color: var(--accent-color); }
